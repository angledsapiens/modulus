export default function Footer() {
  return (
    <footer
      style={{
        marginTop: "48px",
        fontSize: "12px",
        opacity: 0.5
      }}
    >
      Liquidity Elasticity represents response between consecutive
      observations and may be undefined when no state change occurs.
    </footer>
  );
}
